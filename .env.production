# Vantage Medical Office — Production (Demo) Defaults
# App starts in demo mode by default (mock data, no backend needed).
# These values are only used if someone toggles demo mode off in Settings.

VITE_API_BASE_URL=
VITE_S3_BUCKET=vantage-uploads
VITE_S3_REGION=us-east-1
VITE_ZOOM_PHONE_NUMBER=
