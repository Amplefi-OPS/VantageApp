/**
 * Seed script: inserts dummy appointments into DynamoDB for testing.
 *
 * Usage (from project root):
 *   npx tsx infra/scripts/seed-appointments.ts <provider_id>
 *
 * Requires: @aws-sdk/client-dynamodb and @aws-sdk/lib-dynamodb
 * If running outside infra/, install them first or use the shell version below.
 *
 * Shell alternative (no extra deps):
 *   Run: bash infra/scripts/seed-appointments.sh <provider_id>
 */

// This file is kept for reference. Use seed-appointments.sh instead.
